APP_STL := gnustl_static
APP_ABI := arm64-v8a
NDK_TOOLCHAIN_VERSION := clang
#set the APP_PLATFORM to match your platform version (in my case it's Android 6.0 - Marshmallow)
APP_PLATFORM := android-23